# Copy this file to ".streamlit/secrets.toml" for local testing,
# OR paste its contents into the "Secrets" box in Streamlit Cloud's
# app settings (Settings -> Secrets). Do NOT commit the real
# secrets.toml file to GitHub.

GROQ_API_KEY = "your_groq_api_key_here"
